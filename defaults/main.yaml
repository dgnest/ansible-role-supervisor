---
# defaults file for supervisor

supervisor_programs:
  - program: name
    command: 'command'
    user: "user"
    directory: "directory"
    numprocs: 1
    autostart: true
    autorestart: true
    startsecs: 10
    priority: 990
